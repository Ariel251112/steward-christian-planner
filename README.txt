STEWARD Android PWA v1.0.0

For installable Android behavior, deploy the entire folder to an HTTPS static host.
Then open the HTTPS URL in Android Chrome and choose Install app / Add to Home screen.

Files: index.html, manifest.json, service-worker.js, icons/, STEWARD_PROJECT.md

User content is stored in browser localStorage. Use Export JSON Backup regularly.
